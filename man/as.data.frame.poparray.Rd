% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/poparray_class.r
\name{as.data.frame.poparray}
\alias{as.data.frame.poparray}
\title{Coerce poparray to data frame (EAGER)}
\usage{
\method{as.data.frame}{poparray}(
  x,
  stringsAsFactors = TRUE,
  responseName = data_col(x),
  bytes_threshold = 4e+07,
  ...
)
}
\arguments{
\item{x}{A poparray.}

\item{stringsAsFactors}{Passed to \code{as.data.frame.table()}.}

\item{responseName}{Name of the value column (defaults to \code{data_col(x)}).}

\item{bytes_threshold}{is the number of bytes to warnn you shen a large data frame isbing retunrd. Default is 40 MB.}

\item{...}{Passed to \code{as.data.frame.table()} (rarely needed).}
}
\value{
A data.frame with one column per dimension plus \code{responseName}.
}
\description{
S3 method to coerce a poparray to a data frame. This method \strong{realizes} the delayed backend (for the current
poparray slice) and converts it to a long data.frame via \code{as.table()} semantics (one row per cell).
}
\details{
For large cubes, subset first (e.g., restrict years/areas) to avoid materializing an unmanageably large array.
}
