% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cube_io.R
\name{save_poparray}
\alias{save_poparray}
\title{Save a poparray cube to HDF5 using the canonical schema}
\usage{
save_poparray(
  x,
  filepath = NULL,
  overwrite = FALSE,
  chunkdim = "auto",
  level = 6L,
  series_id = NULL,
  geo = NULL,
  extendable_year = NULL,
  registry = NULL,
  target_chunk_bytes = 1e+06
)
}
\arguments{
\item{x}{A \code{poparray}.}

\item{filepath}{Output HDF5 file path. When \code{NULL}, writes to
\code{file.path(cube_path(create = TRUE), "base", paste0(series_id, ".h5"))}.}

\item{overwrite}{Logical; overwrite if file exists.}

\item{chunkdim}{Integer chunk dimensions or \code{"auto"}.}

\item{level}{Compression level (0-9).}

\item{series_id}{Optional series identifier stored as \code{cube/metadata/series_id}.}

\item{geo}{Optional geography tag stored as \code{cube/metadata/geo}.}

\item{extendable_year}{Optional flag stored as \code{cube/metadata/extendable_year}.}

\item{registry}{Optional registry metadata list/data.frame (one row).}

\item{target_chunk_bytes}{Target bytes for auto chunking.}
}
\value{
Invisibly returns a list with \code{filepath}, \code{dataset}, and \code{chunkdim}.
}
\description{
Save a poparray cube to HDF5 using the canonical schema
}
