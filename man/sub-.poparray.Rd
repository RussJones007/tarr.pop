% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/poparray_class.r
\name{[.poparray}
\alias{[.poparray}
\title{Subset a poparray}
\usage{
\method{[}{poparray}(x, ..., drop = FALSE)
}
\arguments{
\item{x}{A poparray.}

\item{...}{Indices, either positional (like base arrays) or named by dimension (e.g., \code{x[year = "2020", sex = "Female"]}). Missing indices in positional form are treated as \code{TRUE} (select all).}

\item{drop}{Logical; passed to the backend \code{[} call.}
}
\value{
A \code{poparray} or (if \code{drop = TRUE} drops time/area) a \code{DelayedArray}.
}
\description{
Subsets the poparray object and updates the stored dimnames metadata. By default (\code{drop = FALSE}) the result remains
a \code{poparray}. If \code{drop = TRUE} and subsetting would drop either the \code{dimroles} time or area dimensions , the
method returns the underlying subsetted \code{DelayedArray} instead of a \code{poparray}.
}
