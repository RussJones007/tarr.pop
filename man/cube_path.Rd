% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cube_storage.R
\name{cube_path}
\alias{cube_path}
\title{Return the active cube storage root}
\usage{
cube_path(create = FALSE, legacy_ok = TRUE)
}
\arguments{
\item{create}{Logical; create the resolved directory if needed.}

\item{legacy_ok}{Logical; allow fallback to bundled \code{inst/extdata}.}
}
\value{
Absolute path to the active cube directory.
}
\description{
Reads the configured cube path from, in order:
\enumerate{
\item \code{getOption("tarr.pop.cube_path")}
\item a YAML config file in the user config directory
\item a package-managed default under \code{tools::R_user_dir()}
}
}
\details{
In interactive sessions, when no configuration exists yet, the user is
prompted for a path and the choice is persisted. In non-interactive sessions,
the default is used unless \code{legacy_ok = TRUE} and package \code{inst/extdata}
exists, in which case that directory is returned as a compatibility fallback.
}
