% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingestion.R
\name{add_population_data}
\alias{add_population_data}
\title{Add population records to an existing cube}
\usage{
add_population_data(
  cube,
  reader,
  transformer = function(df, ...) df,
  dims = NULL,
  add_dim = "year",
  output_filepath = NULL,
  overwrite = FALSE,
  completion_policy = c("error", "zero", "na"),
  drop_all = TRUE,
  source_meta = list(),
  chunkdim = "auto",
  level = 6L,
  target_chunk_bytes = 1e+06,
  data_col = NULL,
  support = NULL,
  ...
)
}
\arguments{
\item{cube}{Existing cube filepath or \code{series_id}.}

\item{reader}{Function returning the new source table.}

\item{transformer}{Function transforming reader output into canonical columns.}

\item{dims}{Character vector of dimension column names. Defaults to the
existing cube dimension order.}

\item{add_dim}{Dimension that receives new labels. Defaults to \code{"year"}.}

\item{output_filepath}{Destination HDF5 filepath. Defaults to replacing
\code{cube} when \code{cube} is a filepath.}

\item{overwrite}{Logical; overwrite \code{output_filepath} when writing to a
different existing file. Replacing the source file is always done through a
temporary file.}

\item{completion_policy}{Completion policy for missing cells in the new
slice: \code{"error"}, \code{"zero"}, or \code{"na"}.}

\item{drop_all}{Logical; remove rows containing aggregate aliases.}

\item{source_meta}{Optional source metadata overrides for the rewritten cube.}

\item{chunkdim}{Chunk dimensions or \code{"auto"}.}

\item{level}{Compression level passed to HDF5 writer.}

\item{target_chunk_bytes}{Target bytes for auto chunk sizing.}

\item{data_col}{Name of the value column. Defaults to the existing cube's
stored data column.}

\item{support}{Optional support table for the new data. Defaults to the
existing cube support crossed with the new \code{add_dim} labels.}

\item{...}{Additional arguments forwarded to \code{reader()} and \code{transformer()}.}
}
\value{
Invisibly returns the normalized output filepath.
}
\description{
Reads and transforms new tabular population data, validates it against an
existing cube's dimensional contract, appends new labels on one dimension,
and writes a replacement cube. The existing HDF5 data are kept lazy while the
combined cube is written to disk.
}
\details{
This is intended for updates such as adding a newly released estimate year to
a county-by-age-by-sex-by-race cube. It does not modify the existing HDF5
dataset in place; when \code{output_filepath} is the same as \code{cube}, a temporary
file is written first and then moved into place.
}
