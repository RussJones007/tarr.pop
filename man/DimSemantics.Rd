% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dim_semantics.r
\docType{class}
\name{DimSemantics}
\alias{DimSemantics}
\title{DimSemantics S7 Class}
\description{
Declarative semantic contract for a single array dimension.
}
\details{
\code{DimSemantics} stores intrinsic semantics only and must not store current-state
facts such as whether overlaps are currently present in a filtered cube.
}
\section{Fields}{

\itemize{
\item \code{dim_name}: Character scalar. Dimension name in the array (e.g., \code{"race"}).
\item \code{domain}: Character scalar semantic key (e.g., \code{"age"}, \code{"race"}).
\item \code{scale_type}: Character scalar in \code{c("nominal", "ordinal", "interval")}.
\item \code{partition_type}: Character scalar in \code{c("partition", "set", "unknown")}.
\item \code{validated}: Non-missing logical scalar.
\item \code{overlap_levels}: Character vector of known overlap-causing levels.
\item \code{notes}: Free-text character vector.
}
}

\section{Validation Rules}{

\itemize{
\item \code{dim_name} must be non-missing, length 1, and non-empty.
\item \code{domain} must be non-missing, length 1, and non-empty.
\item \code{scale_type} must be one of \code{nominal}, \code{ordinal}, \code{interval}.
\item \code{partition_type} must be one of \code{partition}, \code{set}, \code{unknown}.
\item \code{validated} must be non-missing logical(1).
\item If \code{partition_type == "partition"}, then \code{overlap_levels} must be empty.
}

\code{overlap_levels = character()} does not imply unconditional safety. Poparray
guards may still consider context such as level count or interval overlap tests.
}

\keyword{internal}
