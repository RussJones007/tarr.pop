% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/open_pop_array.r
\name{rebuild_poparray_registry}
\alias{rebuild_poparray_registry}
\title{Rebuild the lightweight poparray registry}
\usage{
rebuild_poparray_registry(root = resolve_cube_dir())
}
\arguments{
\item{root}{Cube storage root. Defaults to the active \code{\link[=cube_path]{cube_path()}}.}
}
\value{
A data.frame with one row per registered series.
}
\description{
Scans the active cube storage \verb{base/} directory for HDF5 cubes, reads only
the metadata needed to identify and locate each series, and writes the
lightweight registry to \code{cache/cube_registry.rds}.
}
\details{
The authoritative dimensional metadata and \code{DimSemantics} remain inside each
HDF5 cube and are not duplicated into the registry.
}
