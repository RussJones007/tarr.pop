% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/poparray_class.r
\name{by.poparray}
\alias{by.poparray}
\title{Apply a function by poparray dimension groups}
\usage{
\method{by}{poparray}(data, INDICES, FUN, ..., simplify = TRUE, drop = FALSE)
}
\arguments{
\item{data}{A \code{poparray}.}

\item{INDICES}{Dimension selector used for grouping. Accepts a single
dimension name, role alias (\code{"time"} or \code{"area"}), or a single integer
dimension position.}

\item{FUN}{Function applied to each grouped slice.}

\item{...}{Additional arguments passed to \code{FUN}.}

\item{simplify}{Logical; if \code{TRUE} (default), attempt to simplify results
with \code{base::simplify2array()}.}

\item{drop}{Logical; passed to \code{\link[=split.poparray]{split.poparray()}} and then \verb{[.poparray} for
each slice.}
}
\value{
A named list of results, or a simplified array/vector when
\code{simplify = TRUE}.
}
\description{
Splits a \code{poparray} by one dimension (or role alias) and applies \code{FUN}
to each slice.
}
